<h1>Eat a burger!</h1>

<h2>Try a burger!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers I've Eaten</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Make a New Burger!</h2>
<form class= "create-form">
<div class= "form-group">
    <label for = "newburger">What kind of burger?</label>
    <input type="text" id="newburger" name="burger_name">
</div>

<button type="submit">Send to the kitchen!</button>

</form>